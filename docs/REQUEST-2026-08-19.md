# Request log — 2026-08-19

Recorded verbatim (summarized, not edited for content) per explicit instruction:
every response in this session must keep this repository's documentation in
sync with the conversation, starting with a recap of the request itself.

## What was asked

1. Sync both `f4` and `libwinescape` from GitHub before doing anything else.
2. A critique of the raw-syscall-under-Wine approach was posted to
   [FarManager/FarManager#1123](https://github.com/FarGroup/FarManager/issues/1123)
   (the upstream Far Manager 3 issue proposing this same technique, filed by
   the same author). Evaluate whether the critique is actually relevant, and
   add whatever real protections it points to that don't already exist —
   only where the risk is real, not reflexively.
3. A second attachment contained a `ROADMAP.md` draft and a VFS/IPC feature
   list, apparently produced by a different, less-informed model, with
   relevance not yet assessed. Read it for anything genuinely worth keeping.
4. Formulate an explicit, written **scope boundary** for `libwinescape` so it
   doesn't grow without limit. The stated criterion, in the requester's own
   words:
   - **In scope:** anything that affects Far/f4's user experience under
     Wine — speed, drive-letter/backslash artifacts, situations where Wine
     itself is buggy, file permissions, and similar.
   - **Out of scope by default:** networking that already works the same way
     through WinAPI — *unless* it measurably affects speed, in which case it
     matters after all.
   - **Out of scope:** dragging in POSIX surface for completeness's own
     sake, disconnected from an actual UX improvement.
   - This needs to be a real, checkable boundary recorded in the repo's
     documentation, not an informal understanding.
5. Answers are expected as `git am`-ready patches. Every response in this
   session updates this repository's documentation to match the state of
   the conversation — this file is the first instance of that rule, and it
   is expected to keep being followed, not just applied once.

## What was already true in the repository before this session

Checked before acting, not assumed: the repository already contains
`docs/STATUS-signal-safety-review.md`, `docs/threading.md`, and a working
`cmd/soaktest` — i.e., the empirical signal-safety/scheduler work that the
FarManager issue thread's own comments describe ("аудит и серию
стресс-тестов") had already happened by the time this request arrived. This
session's job on the critique is therefore to check that existing work
actually answers each point raised, not to redo it from scratch.
